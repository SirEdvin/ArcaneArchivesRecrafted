# 0033 — Gem Cutter ordinary flint-and-steel returns

Status: implemented and headlessly verified under delegated behavior-decision authority; connected-player acceptance remains open.

Pinned upstream release `bb99accf48ed583e29b0efae56e28c963407b8df`, `recipe/gct/GCTRecipe.java:handleItemResult`, damages flint-and-steel once and routes it table → player → world. `init/RecipeLibrary.java` uses it in Devouring Charm and Phoenixway recipes. Those items/abilities are not implemented by this increment.

Support ordinary unenchanted flint-and-steel in the existing paid menu. Prepare damage on a copy, reserve return/output capacity, and publish only after the existing final access/inventory/catalog checks. Creative players and native non-damageable (unbreakable) stacks retain their tool. A tool reaching native maximum durability is consumed; no tool break sound, statistics or advancement callback is fired during preparation. Native break effects and enchanted-tool damage processing remain unfinished. Enchanted tools are refused rather than bypassing Unbreaking or invoking effects before payment. Use the native modern maximum-durability boundary, not a manually retained legacy off-by-one lifetime.

All four targets. This bounded implementation accepts the vanilla flint-and-steel item only; custom subclasses/capability combinations remain unsupported. No dependency, resource, save or packet format changes. Uses the existing 0032 inventory-return routing and capacity rejection instead of world surplus drops. Failed crafts neither damage nor consume the live tool. Ordinary non-flint tools continue to be ingredients, not catalysts. Copies preserve unrelated item data. No cross-file crash journal or arbitrary callback sandbox is implied.

Alternatives rejected: damaging the live stack before output can fit, applying unconditional damage to enchanted tools, dropping returns before payment, or retaining the blanket refusal of ordinary flint. Full enchantment/break effects and surplus routing remain migration scope, not approved removals.

Verification: targeted red test failed with the original refusal (`build/flint-red-20260908-135940.log`, one assertion failure). Full green matrix `build/flint-returns-20260908-140131.log`: exit 0 in 35s, 174 tests per Fabric leaf and 186 NeoForge, zero failures/errors/skips; Forge test compilation only. All four production/source artifact pairs and whitespace checks pass. Tests exercise ordinary damage, successful final-use break, full-cursor and late-access nonmutation, combined fluid/tool returns, creative and unbreakable copy preparation, metadata preservation, and real Unbreaking rejection. Creative player interaction and break effects are not runtime-validated; no new client/server runs are claimed.

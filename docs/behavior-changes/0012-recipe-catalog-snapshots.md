# 0012 — Gem Cutter recipe catalog snapshots

Status: implemented under delegated behavior authority; runtime integration pending.

Continuation: `replaceRecipe(GCTRecipe)` now preserves the checked replacement semantics of upstream `GCTRecipeList.java:57-64,75-82,93-100`: missing IDs throw `IndexOutOfBoundsException` instead of silently adding recipes. The typed definition replaces the factory arguments, consistent with 0011; `addRecipe` still permits insertion/overwrite. Successful replacement retains order and invalidates the current list cache; rejected/null replacement leaves the catalog and cached snapshot untouched. Two new regressions cover these contracts, old map/list snapshots and a removed target.

Verification: `timeout --foreground 10m ./gradlew :1.20.1-fabric:test :1.21.1-fabric:test :1.21.1-neoforge:test :1.20.1-forge:compileTestJava assemble --no-daemon`, exit 0 in 26s, log `build/checked-replacement-20260907-223112.log`. XML totals: 85 tests on each Fabric/NeoForge target, zero failures/errors/skips; Forge test compilation only. All four artifact pairs pass. No runtime recipe replacement/reload or scripting integration is claimed.

Pinned upstream `80944ce45c6559243d8928cc4b305bf379388652`, `recipe/gct/GCTRecipeList.java:20-46,104-158` uses insertion-ordered recipes, a cached list, name-based replacement/removal, null for missing recipes and empty stacks for invalid output indices. `getRecipes()` exposes the mutable backing map, allowing changes without invalidating the cached list. `util/ItemUtils.java:18-22` compares item/data while ignoring count for output lookup.

Decision: preserve order, replacement-in-place, removal-by-name (including an old recipe object), missing-index behavior, first matching output and item/data-sensitive count-independent lookup. Return an unmodifiable ordered map snapshot and immutable list snapshot; all writes go through validated methods which invalidate the list cache. Null writes fail before mutation. Use an instance-owned catalog, not a process-global singleton, so eventual lifecycle owners can isolate worlds/tests. No lifecycle or runtime registry binding is claimed here. The typed recipe API replaces upstream factory conveniences; script/creator/condition factories remain unfinished rather than silently dropping their semantics.

All four targets are affected. No save or packet format is added. Numeric indices remain presentation-only and must not authorize crafting; future server handlers must resolve and revalidate recipe IDs. No costs, outputs, textures or progression registrations change. Old snapshots remain stable after catalog changes; callers fetch a new snapshot for current recipes. Recipe internals retain the documented 0011 ownership limits.

Alternatives: expose the live map (permits stale list/index lookup); unmodifiable live view (changes during iteration); global singleton (unnecessary lifecycle coupling). Rejected in favor of ordinary Java collections and explicit instance ownership. Authority is the user's documented behavior delegation, not publication permission.

Verification targets: insertion/replacement/removal ordering, stale-object indexing, stable immutable snapshots, invalid-index handling, detached outputs, data-aware lookup and null-write conservation. Runtime recipe registration, reload synchronization, menu/network integration and crafting remain pending.

Executed `timeout --foreground 10m ./gradlew :1.20.1-fabric:test :1.21.1-fabric:test :1.21.1-neoforge:test :1.20.1-forge:compileTestJava assemble --no-daemon`: exit 0, 26s, log `build/gct-catalog-20260907-220510.log`. Five new regression tests; XML totals 70 tests per Fabric/Fabric/NeoForge target with zero failures/errors/skips. Forge test sources compile but Minecraft-backed fixtures are not executed; the existing harness blocker remains. Four artifact pairs pass `scripts/verify_artifacts.py` and `git diff --check` passes. No runtime catalog binding or full crafting is claimed.

# 0102 — Writ of Expulsion and anvil target binding

Status: implemented and assembly/package-verified on all four targets. Source-backed restoration; no new gameplay policy proposed. Live anvil and multiplayer acceptance remains open.

Release bb99accf48ed583e29b0efae56e28c963407b8df: items/WritOfExpulsionItem.java, events/AnvilHandler.java:18–36, init/RecipeLibrary.java:102–110. Original writ requires creator attribution, an anvil-resolved target UUID/name, creator identity matching the user and current target membership owned by that user. Successful removal consumes one writ and uses the same succession/disband logic as resignation, including self-targeting. Missing membership must reject rather than copy the original null dereference.

Original anvil repair resolves the output custom name through the server profile cache, then saves expel_name and expel. No custom name or an unresolved lookup leaves existing target data unchanged, as upstream. Tooltips continue showing the actual stored target rather than inferring it from the display name. No new target, expiry, revocation, or privilege rule is invented.

Modern native ItemCombinerMenu.quickMoveStack transfers before onTake, passing the emptied/reduced stack afterward. Binding at AnvilMenu.createResult RETURN updates only the server-owned result stack before native delivery; it does not grant output, change costs, replace input, replay interactions or bypass mayPickup. Client never resolves profiles. Live anvil payment, shift-click, failed lookup, protection compatibility and third-party recipe output still require runtime acceptance.

Restore original item/recipe/creative registration, 64-tick BOW use, original model/texture/atlas/text, creator-bound owner-only recipe condition, same-world member and expelled-player notifications, saved-data flush and current-membership authorization. Retain existing malformed-data, server-thread, spectator and live-player safeguards. Expulsion does not itself implement shared storage routing.

Verification: `timeout --foreground 10m ./gradlew assemble :1.21.1-neoforge:test --tests '*HiveExpulsionTest' --tests '*HiveResignationTest' --tests '*HiveInvitationTest' --tests '*HiveCraftingConditionsTest' --tests '*GemCutterDataRecipeTest' --tests '*GemCuttersTableCraftingTest' --tests '*GemCuttersTableMenuTest' --offline --no-daemon`, exit 0, 29s, `build/hive-expulsion-20260910-064808.log`. All four targets assembled. NeoForge XML confirms 62 tests, no skips/failures/errors. Four new expulsion cases cover original name-binding/failed-rename behavior, attribution preservation, removal/self-succession/disband, stale/foreign/missing targets, wrong user/creator, and recipe wire/creator preservation. These are detached fixtures with an explicit profile-resolver test seam, not native anvil interaction tests.

`timeout --foreground 60s python3 scripts/verify_artifacts.py`, exit 0, 0s, `build/hive-expulsion-artifacts.log`: four production/source pairs pass, including recipe, original artwork, atlas and common mixin registration. Both artwork files separately match the release pin byte-for-byte; no PNG animation sidecar exists at that pin. Packaged Fabric mixin method is `method_24928` on both versions; Forge refmap maps createResult to `AnvilMenu;m_6640_()V`. This is mapping/package evidence, not live mixin application.

No client/server launched. Live anvil normal/shift-click output, costs, online/offline profile resolution, resource reload, native item use, notifications, protections, multiplayer and world restart remain acceptance work. Shared storage and Manifest/Brazier integration remain unfinished.

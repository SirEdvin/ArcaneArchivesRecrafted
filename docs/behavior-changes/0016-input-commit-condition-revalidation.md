# 0016 — Input commit condition revalidation

Status: implemented under delegated behavior authority; runtime condition binding pending.

Recipe-level follow-up: three integration regressions exercise the recipe overload with Hive invitation/expulsion policies, changing membership during matching, a throwing membership lookup, confirmed nonmembership, and zero-ingredient recipes with condition/argument validation. Upstream policy was rechecked in pinned `init/RecipeLibrary.java:84-115`. These fixtures use explicit test membership state, not a live Hive save implementation. No production correction was required and no new behavior decision was introduced. The same scoped matrix command below passed in 21s, log `build/hive-commit-integration-20260907-224842.log`: 96 tests each on Fabric/Fabric/NeoForge, zero failures/errors/skips; Forge test compilation only. Four artifact pairs and whitespace checks pass. This supersedes the 93-test baseline, not its runtime limitations.

Pinned upstream `80944ce45c6559243d8928cc4b305bf379388652`, `recipe/gct/GCTRecipeWithConditionsCrafter.java:19-33` stores predicates and short-circuits craftability on the first false condition. Its condition evaluation is separate from matching/consumption. Decision 0005 already requires current conditions and inventory at commit rather than trusting a preview.

Decision: add `consume(matcher, BooleanSupplier)` to the owned input handler and a corresponding `GCTRecipe.consumeIngredients` overload. Snapshot inputs before evaluating the initial condition; reject false before invoking ingredient predicates. After preparing a complete allocation, evaluate conditions again, then compare every live input with the original snapshot before any write. No callbacks run between owned writes. False returns no consumption result; exceptions propagate before deductions. Null callbacks are rejected. Existing unconditional overloads remain explicitly low-level input-only operations, not authorization.

Conditions must read current authoritative server state without effects. A cached boolean or client-provided membership does not satisfy this contract. This is not a sandbox for arbitrary Java callbacks: their external mutations cannot be rolled back, and inventory changes they make remain visible rather than being overwritten with stale snapshots. Final inventory validation detects changed count/item/data. The check and writes require the owning server thread; no cross-thread atomicity is promised.

All four targets are affected. No recipe costs, outputs, assets, persistence format or packets change. The new callback API and repeated evaluation are a documented adaptation under delegated authority. Alternatives: check only before matching (misses changes during custom predicates); check after live inventory validation (callback could invalidate that validation); roll back callback effects (unsafe and beyond the owned boundary). This implements the condition gate for input consumption, not recipe-owned condition lists, hive resolution, output delivery, remainders, or full crafting authorization.

Four regression tests cover initial rejection before matching, permission changes during matching, final callback inventory mutation, exceptions/null callbacks before deductions, and successful guarded consumption. Verification: `timeout --foreground 10m ./gradlew :1.20.1-fabric:test :1.21.1-fabric:test :1.21.1-neoforge:test :1.20.1-forge:compileTestJava assemble --no-daemon`, exit 0 in 28s, log `build/condition-commit-20260907-224418.log`. XML: 93 tests each on both Fabric targets and NeoForge, zero failures/errors/skips; Forge test-source compilation only. All four artifact pairs and `git diff --check` pass. Forge Minecraft-backed harness remains unresolved; no gameplay acceptance claimed.

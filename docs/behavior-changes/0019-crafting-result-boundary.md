# 0019 — Guarded input consumption and detached crafting result

Status: implemented under delegated behavior authority. Full crafting integration remains unfinished.

Follow-up integration verification: three additional regressions exercise the output-return path when the final condition returns false or throws, a matcher changes live input data without changing its count, or the final condition removes and restores the same catalog object. No output is returned and no recipe deductions occur. Deliberate callback mutations remain visible; this is not rollback of arbitrary Java side effects. Existing production guards passed without modification. The same scoped matrix command below exited 0 in 22s at `build/crafting-late-failure-20260908-055535.log`: 106 tests per Fabric/NeoForge target, zero failures/errors/skips, Forge test compilation only. All four artifact/source pairs and whitespace checks passed. Earlier totals below remain historical.

Upstream: `80944ce45c6559243d8928cc4b305bf379388652`, `recipe/gct/GCTRecipe.java:97-109,114-159` exposes output separately and performs sequential ingredient extraction and return handling. The port's approved 0005 requires complete consumption before granting output; 0010/0015 require copy-safe creator metadata, and 0017/0018 guard current recipe definitions.

Decision: add catalog `consumeForCraft`, validating required arguments and preparing a detached creator-policy output before input mutation. It returns `GCTCraftingResult` only after existing guarded consumption succeeds. Missing recipes, insufficient inputs, failed conditions and catalog writes yield no result; callback exceptions propagate before owned deductions. Result accessors return copies of output and each consumed stack. All consumed input data remains available for subsequent container/tool/fluid remainder handling. No arbitrary output callback is introduced. The shared class uses the existing creator API and consumption path instead of duplicating matching logic.

The owner must resolve identity and conditions from server state, call on the owning server thread, persist consumed input state and arrange output/remainder delivery. This in-memory return value is not a durable pending-output slot, single-use capability, inventory reservation or complete crash-safe transaction. Repeated reads produce copies, not permission to deliver repeatedly. Output preparation is side-effect-free in the current final recipe definition; future extensibility must preserve that boundary. Output delivery, full remainders, device/menu/network registration and gameplay remain pending.

Targets: Fabric 1.20.1/1.21.1, Forge 1.20.1, NeoForge 1.21.1. No recipes, costs, textures, save/network formats or dependencies change. The return API is a documented modernization, not legacy binary compatibility. Alternatives: grant output before consumption (unsafe), discard consumed stacks (loses remainder data), or implement external delivery before its storage owner exists (premature).

Verification: two integration tests cover creator stamping, detached outputs/consumed data, insufficient inputs, false/throwing conditions, null identity, missing recipes, replacement rejection, and subsequent ordinary zero-input output. Command `timeout --foreground 10m ./gradlew :1.20.1-fabric:test :1.21.1-fabric:test :1.21.1-neoforge:test :1.20.1-forge:compileTestJava assemble --no-daemon`, exit 0 in 22s; log `build/crafting-result-final-20260908-054853.log`. XML totals: 103 tests on each Fabric/NeoForge leaf, zero failures/errors/skips. Forge test compilation only; its Minecraft-dependent JUnit harness remains unresolved. Four artifact/source pairs and `git diff --check` pass. No runtime crafting acceptance is claimed.

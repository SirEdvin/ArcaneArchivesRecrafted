# 0.0.3 implementation evidence

All ten requested changes have implementation in the working tree. Behavior adaptations are recorded separately in 0155–0164. No new production dependency, publication, commit or push was introduced.

## Automated verification

### Subsequent Brazier pull-mode addition

Approved behavior 0165 adds Deposit/Pull mode switching, copied exact filter selection, no-filter inactivity and a persistent one-stack output buffer. Native hopper extraction runs through each loader's actual automation path. Chest/Trove source authorization/radius, capacity, simulation, mode/filter retention, serialization and removal conservation pass in the shared fixture. Manipulation now toggles mode; sneak-Manipulation retains the configuration menu. Updated tooltip, Tome pages and release notes describe the controls.

Latest verification, superseding the earlier artifact build below: `timeout --foreground 10m ./gradlew build verifyReleaseArtifacts --no-daemon`, exit 0, 81s, `build/brazier-pull-final-20260914-171051.log`. Tome regeneration and 25 Python tests also pass. No graphical acceptance or actual buffered-Brazier process-restart claim is made.

### Original ten-item release scope

`timeout --foreground 10m ./gradlew build verifyReleaseArtifacts --no-daemon`

- Final successful invocation: exit 0, 78 seconds, `build/003-release-20260914-150842.log`.
- Includes all four supported leaves, their native test wiring, production/source JAR verification and Forge release-mixin verification.
- JUnit reports: Fabric 1.20.1 243 tests; Forge 1.20.1 60 plain-runtime tests plus its native shared suite; Fabric 1.21.1 243; NeoForge 1.21.1 319. All reported failures/errors/skips are zero. These figures are not summed into a gameplay-parity claim.
- Native Release003Lifecycle covers placement for each storage type; exact positive-limit denial; creative bypass and accounting; return to survival; removal/replacement; zero unlimited; conversion denial without source/payment loss; unavailable cross-dimensional entries without chunk loading; owner/type separation; table update packet and persisted input state; normalized growth packet; lectern-origin snapshot transport and player-independent beam geometry.
- Dedicated SavedData tests verify NBT round trips, dimension/type/owner retention, and malformed/duplicate entry rejection. Search tests exercise provider priority and stale-session behavior; configuration tests cover defaults, opt-out and invalid-file preservation.
- `timeout --foreground 2m python3 -m unittest discover -s scripts -p 'test_*.py'`: exit 0, 25 tests, `build/003-python-final.log`.

Earlier failures are retained rather than counted as passes: native fixture replicas initially lacked a level; another position intersected NeoForge's enclosing structure barrier; Tome native resource totals needed updating for the deliberately removed navigation/templates; final artifact allowlists initially lacked the six new production classes. These were corrected at their root, without weakening exact artifact membership checks. `build/003-final-20260914-150536.log` had successful builds/native suites but failed the then-outdated artifact allowlist.

## Graphical evidence

Existing private-Xvfb driver and a disposable copy of an archived test world were reused. Evidence directory: `/tmp/arcane-003-live-eui2qnh1/`. First real Fabric 1.21.1 + EMI session exited normally with code 0 in 459.32 seconds; full log `build/release-003-live-1789398150282956863.log`.

The first top-down capture verified one item per occupied input regardless of counts 1/64/32, but exposed incorrect outer-slot spacing. The renderer was corrected to the model's smaller central grid (centers .36/.50/.64) and reduced in scale/height. The final automated matrix above includes this fix.

A subsequent real client/integrated-server restart retained the three stored stacks, and the top-down capture verified the corrected diagonal grid alignment. The Tome opened successfully with the three authored categories. Patchouli also supplies its own built-in searchable Entry Index button; no redundant authored Index/Home/Table of Contents category remains. Native navigation/search is retained rather than modifying Patchouli internals to suppress its own index.

The Radiant Chest presentation was opened and its large actual block preview visibly rendered beside the prose. Screenshot: `/tmp/arcane-003-live-eui2qnh1/tome-chest-preview.png`. The second session closed normally, exit 0 in 240.35 seconds, `build/release-003-live-1789398624799730088.log`. Both test clients were stopped. This is Fabric 1.21.1 + EMI integrated-client evidence, not the full remote multiplayer matrix.

## Explicitly deferred acceptance

- Exhaustive visual acceptance of chest overlays, quartz growth/completion, Tome previews and volumetric beams across all loaders, GUI scales and camera angles.
- Connected two-client observer updates and separate users' lectern/handheld tracking.
- Actual installed-viewer UI combinations EMI-only, JEI-only, both and neither across every leaf. Callback priority/lifetime tests are not equivalent to all these live combinations.
- Full process-restart and chunk unload/reload campaign for the quota ledger; NBT round trips/unavailable-coordinate/native placement tests are not a substitute for that campaign.
- Exhaustive protection-mod/Forge snapshot cancellation and every explosion/command/portable-placement path with quota assertions.
- No legacy-storage migration/backfill or over-limit cleanup is implemented, per the approved scope.

These are verification limitations, not missing renderer/search/quota implementations. Earlier migration caveats remain in effect.
